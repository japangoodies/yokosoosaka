name = "yokoso-stock-proxy"
main = "index.js"
compatibility_date = "2025-04-01"

triggers = { crons = ["0 * * * *"] }

# KV Namespace binding (required for orders + product sync)
kv_namespaces = [
  { binding = "ORDERS_KV", id = "5b69cc8ac81b403bbae92adf321879a9" }
]
